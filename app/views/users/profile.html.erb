<div class="container mt-3 text-center">
  <div class="card-profile">
    <h1>Profile</h1>
    <p><%= "Name: #{@user.name}" %></p>
    <p><%= "Email: #{@user.email}" %></p>
    <p><%= "Reviews left: #{@user.reviews.count}" %></p>
    <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-yellow" %>
  </div>
</div>
